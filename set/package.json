{
  "name": "@openflow/set",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "set[flow]: the session manager — the clip grid, the mixer and the device chain, in React."
}
