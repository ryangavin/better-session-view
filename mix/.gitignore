node_modules/
dist/
electron/dist/
release/
coverage/
report/
.DS_Store
# fetched, pinned and verified by tools/prepare.ts: uv, yt-dlp, the LGPL FFmpeg pair and
# the native helpers. Binaries, so they are downloaded or built rather than committed
bin/
# generated by npm run warp -- --report: what the beat finding saw, plus hard links to
# the stems, for the harness page. The truth folder is hand-corrected beats, and is kept
harness/reports/*
!harness/reports/truth/
# local key comparison binaries/venv; never packaged or committed
.key-experiments/
