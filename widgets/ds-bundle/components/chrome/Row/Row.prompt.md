Row from bsv-widgets. Use via `window.BsvWidgets.Row` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface RowProps {
  children?: React.ReactNode;
  /** Space between controls, in px. */
  gap?: number;
  className?: string;
}
```
