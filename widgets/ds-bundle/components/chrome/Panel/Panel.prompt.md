Panel from bsv-widgets. Use via `window.BsvWidgets.Panel` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface PanelProps {
  children?: React.ReactNode;
  /** Number of aligned sections in every column. */
  rows: number;
  /** Space between columns, in px. */
  gap?: number;
  className?: string;
}
```

## Related

`PanelColumn`
