Label from bsv-widgets. Use via `window.BsvWidgets.Label` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface LabelProps {
  children: React.ReactNode;
  /** Section headings sit above a group; a plain label sits under a control. */
  heading?: boolean;
  className?: string;
}
```
