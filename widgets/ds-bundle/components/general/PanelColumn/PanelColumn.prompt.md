PanelColumn from bsv-widgets. Use via `window.BsvWidgets.PanelColumn` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface PanelColumnProps {
  children?: React.ReactNode;
  className?: string;
}
```
