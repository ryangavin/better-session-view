Divider from bsv-widgets. Use via `window.BsvWidgets.Divider` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface DividerProps {
  orientation?: "horizontal" | "vertical";
  className?: string;
}
```
